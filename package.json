{
  "name": "leaflet-areacodecluster",
  "version": "1.0.0",
  "description": "A leaflet plugin for marker clustering based on area code",
  "main": "dist/leaflet-areacodecluster.js",
  "scripts": {
    "build": "rollup -c",
    "gh-pages": "rollup -c;mkdir -p public;cp -r -t public dist example README.md",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/frogcat/leaflet-areacodecluster.git"
  },
  "keywords": [
    "leaflet",
    "javascript",
    "cluster"
  ],
  "author": "frogcat",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/frogcat/leaflet-areacodecluster/issues"
  },
  "homepage": "https://github.com/frogcat/leaflet-areacodecluster#readme",
  "devDependencies": {
    "@rollup/plugin-buble": "^0.21.3",
    "rollup": "^2.24.0",
    "rollup-plugin-terser": "^7.0.0"
  }
}
